/**
 * Datenbank-Client — vorbereitet, ohne Implementierung.
 * Später z. B. Drizzle/Prisma + Connection-String aus process.env.
 */

export function getDb() {
  throw new Error("Datenbank noch nicht konfiguriert.");
}
