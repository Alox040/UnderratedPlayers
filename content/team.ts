export type TeamMember = {
  name: string;
  role: string;
  bio: string;
};

/** Platzhalter — später durch echte Teamdaten ersetzen. */
export const team: TeamMember[] = [
  {
    name: "EddyAgent",
    role: "Kernteam",
    bio: "Kurzbeschreibung folgt.",
  },
];
